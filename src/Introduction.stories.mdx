import Button from '@mui/material/Button'
import Stack from '@mui/material/Stack'
import { Meta, Story } from '@storybook/addon-docs'

import pkg from '../package.json'

<Meta title="E2G Design System" />

<h1>{pkg.name}</h1>

This site will serve as both documentation and development environment for shared components and design tokens that comprise the Equity Engineering Group's design system.

# Test Components

Here's some components to test embedding stories into mdx documents.

<Story id="components-button--variant-smattering" />
